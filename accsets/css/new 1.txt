<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh Tìm Kiếm</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }

        .search-container {
            display: flex;
            align-items: center;
            max-width: 600px;
            margin: 0 auto;
            position: relative;
        }

        .search-bar {
            display: flex;
            align-items: center;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            overflow: hidden;
            width: 100%;
        }

        .dropdown {
            position: relative;
            border-right: 1px solid #ddd;
        }

        .dropdown select {
            padding: 10px;
            border: none;
            outline: none;
            background: transparent;
            font-size: 14px;
            color: #333;
            cursor: pointer;
            appearance: none;
            width: 100px;
        }

        .dropdown::after {
            content: "▼";
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 10px;
            color: #333;
            pointer-events: none;
        }

        .search-input {
            flex: 1;
            padding: 10px;
            border: none;
            outline: none;
            font-size: 14px;
            color: #333;
        }

        .search-button {
            background-color: #000;
            border: none;
            padding: 10px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .search-button img {
            width: 20px;
            height: 20px;
        }

        .close-button {
            position: absolute;
            right: 40px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            font-size: 16px;
            cursor: pointer;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="search-container">
        <div class="search-bar">
            <div class="dropdown">
                <select>
                    <option>Tất cả</option>
                    <option>Tìm kiếm sản phẩm...</option>
                </select>
            </div>
            <input type="text" class="search-input" placeholder="Tìm kiếm sản phẩm...">
            <button class="close-button">×</button>
            <button class="search-button">
                <img src="https://via.placeholder.com/20?text=🔍" alt="Search Icon">
            </button>
        </div>
    </div>
</body>
</html>